const test="        function subscribeToLiveScenes(){if(!supabaseClient||!currentSessionId){return}console.log('live_scenes 구독 시작, 세션 ID:',currentSessionId);const channel=supabaseClient.channel('live-scenes-'+currentSessionId).on('postgres_changes',{event:'INSERT',schema:'public',table:'live_scenes',filter:`session_id=eq.${currentSessionId}`},(payload)=>{console.log('새 장면 수신:',payload);if(currentRole==='B'){const sceneText=payload.new.scene_text;if(sceneText){const expSceneText=document.getElementById('expSceneText');if(expSceneText){expSceneText.textContent=sceneText;switchExpGeneratedTab('scene');expCurrentPhase='interpret';const emotionCueMsg=window.lastSceneData?.emotionCue||'이 장면에서 어떤 감정이 느껴져?';addExpChatMessage('ai','화자의 기억이 도착했어. '+emotionCueMsg);const expTextInput=document.getElementById('expTextInput');if(expTextInput){expTextInput.value='';expTextInput.focus()}}}}).subscribe((status)=>{console.log('live_scenes 구독 상태:',status)});window.liveScenesChannel=channel;}";eval(test);